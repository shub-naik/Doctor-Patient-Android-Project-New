<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DoctorPatientAndroidAppNew" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="colorSecondary">@color/purple_500</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorControlHighlight">@color/controlActivated</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->
        <item name="android:windowAnimationStyle">@style/dialog_animation</item>

        <!-- Light Theme Here -->
        <item name="BoldTitleTxtViewColor">@color/red</item>
    </style>

    <style name="dialog_animation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <style name="ButtonStyle">
        <!--        <item name="android:insetBottom">0dp</item>-->
        <!--        <item name="android:insetTop">0dp</item>-->
        <!--        <item name="android:insetLeft">0dp</item>-->
        <!--        <item name="android:insetRight">0dp</item>-->
        <item name="android:textSize">10sp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="ProgressBarColor">
        <item name="colorAccent">@color/purple_500</item>
    </style>

    <!--  Date Picker Styles   -->
    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/purple_500</item>           <!-- Header Background -->
        <item name="android:textColor">@color/purple_500</item>
        <item name="android:buttonBarPositiveButtonStyle">@style/DialogButtonStyled</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/DialogButtonStyled</item>
        <item name="android:colorControlActivated">@color/purple_500</item><!--selected day-->
    </style>

    <!-- Date picker ok/cancel button styles -->
    <style name="DialogButtonStyled" parent="Base.Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/purple_500</item>
        <item name="android:textSize">12sp</item>
    </style>

    <!-- Color Theme for Light Mode -->
    <style name="TextAppearance.App.Widget.TextView" parent="TextAppearance.AppCompat">
        <item name="android:textColor">?attr/BoldTitleTxtViewColor</item>
    </style>

    <!-- For Doctor Login -->
    <style name="DoctorLoginTheme" parent="Theme.DoctorPatientAndroidAppNew">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Patient Login SignUp -->
    <style name="PatientLoginSignUpTheme" parent="DoctorLoginTheme" />

    <style name="SplashScreenTheme" parent="Theme.DoctorPatientAndroidAppNew">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>